const {assert} = require("chai");
// const {exec} = require("..");

describe("exec", function() {
    it("gets context");
    it("throws if returns false");
    it("returns TypeChainable if returns true");
});
